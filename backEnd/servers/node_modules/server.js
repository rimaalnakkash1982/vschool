var express=require("express");
var app=express();
var port=process.env.Port||5000;
app.get("/",function(req,res){
    res.send(("Hello Miro Welcome"))
});
app.listen(port,function(){
    console.log("your port is"+port);
});