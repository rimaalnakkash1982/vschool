var str="bookkeeper larry";
var input=function(string){ 
var duplicate="";
var misfit="";
for( var i=0; i<string.length; i++){
   if{
      (str.indexOf==string[i]) 
       console.log(duplicate.indexOf)}
   }else {
       misfit
       console.log(misfit.indexOf)
   }
    
   console.log (input) 